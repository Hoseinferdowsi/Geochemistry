<div dir="rtl">

# 🐛 باگ‌ها و مشکلات

فهرست باگ‌ها و مشکلات شناخته‌شده. هر باگ فقط پس از رفع و تأیید، وضعیتش به «رفع شد» تغییر کند.

**قالب ثبت باگ:**
- **عنوان:** …
- **تاریخ:** …
- **ماژول / فایل:** …
- **شدت:** `کم` / `متوسط` / `بالا` / `بحرانی`
- **توضیح:** …
- **وضعیت:** `باز` / `در حال بررسی` / `رفع شد`

---

## باگ‌های باز

### B1. ارجاع به فایل ناموجود در gui_app.py
- **تاریخ:** ۲۰۲۶-۰۸-۰۸ | **ماژول:** `gui_app.py`
- **شدت:** بالا
- **توضیح:** `ProcessingThread.run()` اسکریپت `read_duplicates.py` را صدا می‌زند که در پروژه وجود ندارد (فقط `read_duplicate_parallel.py` و `read_duplicates_bkup.py` موجودند). در نتیجه GUI دسکتاپی هنگام پردازش خطا می‌دهد.
- **وضعیت:** باز

### B2. مسیرهای هاردکد در اسکریپت‌های مستقل
- **تاریخ:** ۲۰۲۶-۰۸-۰۸ | **ماژول:** `Anomaly_Detection.py` ،`Draw_Plot.py` ،`normalization_process.py` ،`convert_standard_scale.py`
- **شدت:** متوسط
- **توضیح:** مسیرهای مطلق ماشین توسعه‌دهنده (مانند `D:/03_AI/...` و `Geochemistry_v3.0/...`) هاردکد شده‌اند؛ روی هر سیستم دیگری اجرا نمی‌شوند.
- **وضعیت:** باز

### B3. ناسازگاری ضریب سانسور بین دو مسیر کد
- **تاریخ:** ۲۰۲۶-۰۸-۰۸ | **ماژول:** `Censored_Data.py` در برابر `processing_services.py`
- **شدت:** بالا
- **توضیح:** ضریب مقادیر بزرگ‌تر از حد تشخیص در `Censored_Data.py` برابر **1.33** و در وباپ برابر **1.25** است؛ یعنی پردازش یک فایل یکسان، خروجی‌های متفاوتی می‌دهد.
- **وضعیت:** باز

### B4. کدینگ UTF-16 فایل requirements.txt
- **تاریخ:** ۲۰۲۶-۰۸-۰۸ | **ماژول:** `requirements.txt`
- **شدت:** متوسط
- **توضیح:** فایل با BOM/UTF-16 ذخیره شده؛ در بعضی سیستم‌ها `pip install -r` خطا می‌دهد یا وابستگی‌ها اشتباه خوانده می‌شوند.
- **وضعیت:** باز

### B5. نبود محافظت path traversal در دانلود session
- **تاریخ:** ۲۰۲۶-۰۸-۰۸ | **ماژول:** `geochemistry_analysis_app.py` (route دانلود)
- **شدت:** متوسط
- **توضیح:** مسیر فایل دانلود از ورودی کاربر ساخته می‌شود و فقط `exists`/`isfile` بررسی می‌شود؛ با `../` می‌توان از پوشهٔ session خارج شد. بهتر است با `realpath` داخل پوشهٔ session محصور شود.
- **وضعیت:** باز

### B6. عدم پاک‌سازی sessionها و آپلودها
- **تاریخ:** ۲۰۲۶-۰۸-۰۸ | **ماژول:** وباپ
- **شدت:** کم
- **توضیح:** پوشه‌های `sessions/` و `uploads/` به‌مرور پر می‌شوند و مکانیزمی برای پاک‌سازی خودکار وجود ندارد.
- **وضعیت:** باز

### B7. README قدیمی و ناسازگار با واقعیت پروژه
- **تاریخ:** ۲۰۲۶-۰۸-۰۸ | **ماژول:** مستندات
- **شدت:** کم
- **توضیح:** نسخه 2.6 نوشته شده در حالی که پروژه v3.1 است؛ ساختار فایل‌ها هم قدیمی است.
- **وضعیت:** باز

### B8. مسیر خراب فونت فارسی در قالب اصلی
- **تاریخ:** ۲۰۲۶-۰۸-۰۸ | **ماژول:** `templates/geochemistry_analysis.html`
- **شدت:** کم
- **توضیح:** قالب فونت را از `/static/fonts/IRANSansWeb.woff` لود می‌کند ولی فایل واقعی در `static/IRANSansWeb.woff` است (بدون پوشهٔ fonts). در نتیجه فونت لود نمی‌شود و فونت جایگزین (Arial) نمایش داده می‌شود.
- **وضعیت:** باز

---

**آخرین به‌روزرسانی:** ۲۰۲۶-۰۸-۰۸
</div>
