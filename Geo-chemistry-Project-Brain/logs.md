<div dir="rtl">

# 📜 ثبت عملیات فنی پروژه

اینجا باید تمامی عملیات فنی انجام‌شده در پروژه ثبت شود (فقط عملیات فنی، نه بحث‌ها). قالب هر ورودی:

- **عنوان کار**
- **تاریخ و ساعت**
- **توضیحات مختصر (حداقل ۴ خط)**
- **توسط:** نام ایجنت یا برنامه‌نویس

سپس یک خط کامل افقی مثل `---`

---

## بازسازی ساختار پوشهٔ Project Brain

- **تاریخ و ساعت:** ۲۰۲۶-۰۸-۰۸ — 09:21
- **توضیحات:**
  1. فایل `intro.md` با معرفی کامل پروژه (رابط‌ها، تکنولوژی‌ها، وضعیت سلامت) و راهنمای استفاده از Brain به‌روزرسانی شد.
  2. فایل `roadmap.md` با پیشنهادهای حاصل از بازبینی کامل پروژه پر شد: ۱۲ طرح در ۴ فاز (یکپارچه‌سازی، رفع باگ، بهبودها، مستندات) هرکدام با اولویت و وضعیت.
  3. دو فایل جدید ساخته شد: `ideas.md` برای ثبت ایده‌ها (۷ ایدهٔ اولیه) و `bugs.md` برای ثبت باگ‌ها (۷ باگ شناخته‌شده با شدت و وضعیت). پس از آن، باگ ۸ (مسیر خراب فونت IRANSansWeb در قالب) نیز به‌همان فایل اضافه شد.
  4. پوشهٔ `wiki/` با ۱۱ فایل ساخته شد: فهرست، نمای کلی، معماری، قواعد پروژه و مستندات ۶ گروه ماژول (وباپ، هستهٔ پردازش، Outlier، اسکریپت‌های مستقل، نمونه‌های تکراری، رابط کاربری).
  5. در مستندات، چند یافتهٔ جدید نیز ثبت شد: مسیر خراب فونت IRANSansWeb در قالب، و ایمپورت بی‌استفادهٔ cKDTree در Draw_Plot.py.
- **توسط:** Buffy (Freebuff)

---

## اعمال قانون راست‌چین‌سازی همهٔ اسناد Brain

- **تاریخ و ساعت:** ۲۰۲۶-۰۸-۰۸ — 05:59
- **توضیحات:**
  1. قانون جدید تصویب شد: همهٔ فایل‌های Markdown داخل پوشهٔ Brain (شامل `wiki/` و `wiki/modules/`) باید راست‌چین باشند.
  2. روش اعمال: کل محتوای هر سند داخل `<div dir="rtl">` در ابتدا و `</div>` در انتهای فایل قرار گرفت.
  3. این تغییر روی هر ۱۶ سند Brain اعمال شد (۶ فایل ریشه + ۱۰ فایل ویکی).
  4. قانون در `wiki/conventions.md` (بخش جدید «راست‌چین بودن اسناد Brain») و `intro.md` (قواعد مهم) ثبت شد.
- **توسط:** Buffy (Freebuff)

---

## راه‌اندازی نسخهٔ کنترل (git) و اولین push

- **تاریخ و ساعت:** ۲۰۲۶-۰۸-۰۸ — 06:10
- **توضیحات:**
  1. ریپوی git با شاخهٔ اصلی `main` در ریشهٔ پروژه ساخته شد و `.gitignore` برای حذف فایل‌های حجیم/حساس (Data با ۱.۱ گیگابایت، sessions، uploads، output، venv و…) ایجاد شد.
  2. commit اولیه با ۱۹۲ فایل (۵۱هزار خط) ساخته شد: کد وباپ، اسکریپت‌ها، قالب‌ها، مستندات و کل پوشهٔ Brain.
  3. remote به آدرس https://github.com/Hoseinferdowsi/Geochemistry.git اضافه شد.
  4. تلاش push به دلیل خطای احراز هویت گیتهاب ناموفق ماند («Invalid username or token») — نیاز به ورود کاربر دارد.
- **توسط:** Buffy (Freebuff)

---
</div>
