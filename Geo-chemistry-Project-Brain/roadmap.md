<div dir="rtl">

# 🗺️ نقشهٔ راه (Roadmap)

طرح‌هایی که باید پیاده‌سازی شوند، به‌همراه اولویت و وضعیت.  
این فهرست حاصل **بازبینی کامل پروژه در ۲۰۲۶-۰۸-۰۸** است و به‌مرور با طرح‌های جدید تکمیل می‌شود.

**راهنما:** هر آیتم = یک طرح. وضعیت‌ها: `باز` / `در حال انجام` / `انجام شد` / `رد شد`

---

## 📋 فاز ۱ — یکپارچه‌سازی و پاکسازی (اولویت: بالا)

### R1. نگهداری یک اپ واحد و حذف نسخه‌های تکراری
- **اولویت:** بالا | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- `geochemistry_analysis_app.py` به‌عنوان اپ واحد نگه داشته شود.
- نسخه‌های موازی حذف یا بایگانی شوند: `app.py` (فقط اگر داشبورد نمونهٔ تکراری نگه داشته می‌شود)، `geochemistry_analysis_app2.bkup`، `geochemistry_analysis_app2_old.bkup`.
- قالب‌های تکراری HTML (`geochemistry_analysis_2.html`، `geochemistry_analysis_edited.html`) ادغام و حذف شوند.
- اسکریپت‌های تکراری تحلیل تکراری (`read_duplicates_bkup.py`، `_read_duplicates.py`) با `read_duplicate_parallel.py` مقایسه و یکی شوند.

### R2. حذف اسکریپت‌های مستقلِ دوباره‌پیاده‌سازی‌شده
- **اولویت:** بالا | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- منطق `Censored_Data.py`، `normalization_process.py` و `Draw_Plot.py` داخل `processing_services.py` موجود است → اسکریپت‌های مستقل اضافه‌اند و حذف شوند (بعد از اطمینان از پوشش کامل قابلیت‌ها).

---

## 📋 فاز ۲ — رفع باگ‌ها و ناسازگاری‌ها (اولویت: بالا)

### R3. رفع ارجاع خراب در gui_app.py
- **اولویت:** بالا | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- `gui_app.py` اسکریپت `read_duplicates.py` را صدا می‌زند که **وجود ندارد**. باید به `read_duplicate_parallel.py` (یا اپ واحد) وصل شود.

### R4. یکسان‌سازی ضریب سانسور
- **اولویت:** بالا | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- `Censored_Data.py` برای مقادیر `>` از ضریب **1.33** استفاده می‌کند، ولی وباپ از **1.25** → نتیجهٔ دو مسیر یکسان نیست. باید یک ضریب مرجع تعیین و در همه‌جا استفاده شود.

### R5. رفع کدینگ requirements.txt و تفکیک وابستگی‌ها
- **اولویت:** بالا | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- `requirements.txt` با کدینگ **UTF-16** ذخیره شده و برای pip روی بعضی سیستم‌ها مشکل‌ساز است → تبدیل به UTF-8.
- وابستگی‌های وباپ از PyQt5 (فقط برای GUI دسکتاپی) جدا شوند.

### R6. حذف مسیرهای هاردکد در اسکریپت‌ها
- **اولویت:** متوسط | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- `Anomaly_Detection.py` و `Draw_Plot.py` مسیر `D:/03_AI/AI_Programming/Geochemistry_v3.1/Data/Kashmar_censored_processed.xlsx` را هاردکد کرده‌اند؛ `normalization_process.py` و `convert_standard_scale.py` هم مسیر قدیمی `Geochemistry_v3.0` دارند → مسیرها پارامتری شوند.

---

## 📋 فاز ۳ — بهبودها و ویژگی‌های جدید (اولویت: متوسط)

### R7. ادغام تشخیص آنومالی در وباپ
- **اولویت:** متوسط | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- قابلیت‌های `Anomaly_Detection.py` (روش Mean±SD و روش غلظت-تعداد C-N) به‌صورت مرحلهٔ جدید در pipeline وباپ اضافه شود.

### R8. تکمیل تب «تحلیل چندفاکتوری»
- **اولویت:** متوسط | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- تب ۶ وباپ فعلاً «Coming Soon» است → پیاده‌سازی PCA و Factor Analysis با Scree Plot و Biplot.

### R9. بهینه‌سازی الگوریتم IDW
- **اولویت:** متوسط | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- حلقهٔ دوتایی O(grid² × n) پایتونی در `_idw_interpolation` برای داده‌های بزرگ کند است → برداری‌سازی با NumPy یا استفاده از cKDTree.

### R10. امنیت و پایداری وباپ
- **اولویت:** متوسط | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- `secret_key` هاردکد شده و `debug=True` برای محیط واقعی مناسب نیست.
- محافظت در برابر path traversal در مسیر دانلود session.
- پاک‌سازی خودکار session‌ها و آپلودهای قدیمی.

### R11. زیرساخت: نسخهٔ کنترل و تست
- **اولویت:** متوسط | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- پروژه در حال حاضر **git نیست** → راه‌اندازی مخزن و commit اولیه.
- افزودن تست برای توابع هسته (`outliers.py`، تبدیل‌های نرمال، پردازش سانسور).

---

## 📋 فاز ۴ — مستندات (اولویت: کم)

### R12. به‌روزرسانی README
- **اولویت:** کم | **وضعیت:** باز | **منبع:** بازبینی ۲۰۲۶-۰۸-۰۸
- نسخه و ساختار فایل‌ها در README قدیمی است (2.6 در برابر 3.1؛ اشاره‌ای به `processing_services.py` و `sessions/` ندارد).

---

**آخرین به‌روزرسانی:** ۲۰۲۶-۰۸-۰۸
</div>
